var myName = 'Chris'

function sayName() {
	var secret = 'watch and code';
	console.log(myName);
}

sayName();

//console.log(secret); results in an error
// Scope does not allow you to access data inside a function.  